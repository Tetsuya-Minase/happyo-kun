{
  "name": "happyo-kun",
  "version": "1.0.0",
  "private": true,
  "description": "🎯 Your presentation buddy - Interactive slides with live coding powered by Slidev & Cloudflare",
  "scripts": {
    "dev": "pnpm --filter start dev",
    "build": "pnpm --filter start build && pnpm --filter slide build && cp -r apps/slide/dist apps/start/dist/slide && cp -r apps/slide/functions apps/start/dist/functions",
    "preview": "pnpm --filter start preview",
    "dev:slide": "pnpm --filter slide dev",
    "dev:start": "pnpm --filter start dev"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}